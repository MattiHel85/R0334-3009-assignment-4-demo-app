import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'R0334-3009-assignment-4-demo-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
